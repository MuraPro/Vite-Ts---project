# Используем официальный Node.js образ для контейнера
FROM node:22

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем package.json и package-lock.json в контейнер
COPY package*.json ./

# Устанавливаем зависимости проекта
RUN npm install

# Копируем остальные файлы проекта в контейнер
COPY . .

# Собираем приложение
RUN npm run build

# Указываем команду для запуска контейнера (например, запуск storybook или веб-приложения)
CMD ["npm", "run", "sb"]
